import { AboutContent } from '@/components/about-content'

export const metadata = {
    title: 'About - My Blog',
    description: 'Learn more about this blog and the technologies used to build it',
}

export default function AboutPage() {
    return <AboutContent />
}
